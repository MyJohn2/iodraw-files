```mermaid

flowchart TD
    subgraph 国家层（国家海洋信息中心）
        A1[国家海域海岛动态管理系统应用集群] --> B1[国家应用服务器集群（Tomcat/Nginx）]
        B1 --> C1[国家数据层]
        C1 --> C11[PostgreSQL主数据库（全量全国数据）]
        C1 --> C12[国家Redis缓存（热点数据）]
        A1 --> D[中间件集群]
        D --> D1[Kafka集群（国家-地方同步通道）]
        D --> D2[Debezium-connector-postgres（PostgreSQL变更捕获）]
        D --> D3[自研数据同步程序（数据解析/转换/写入）]
    end
    
    subgraph 网络层
        E[海洋专网（国家-地方加密传输）] --> E1[防火墙/入侵检测系统（安全防护）]
        E1 --> E2[VPN加密通道（跨区域数据传输）]
    end
    
    subgraph 地方层（13个省市海洋局，单省市示例）
        F1[地方海域海岛动态管理系统应用节点] --> G1[地方应用服务器（Tomcat/Nginx）]
        G1 --> H1[地方数据层]
        H1 --> H11[PostgreSQL从数据库（仅本区域数据）]
        H1 --> H12[地方Redis缓存（本地热点数据）]
        F1 --> D[中间件集群]
        H11 --> D2
    end
    
    D2 --> C11[国家PostgreSQL主数据库]
    D2 --> H11[地方PostgreSQL从数据库]
    D1 --> D3
    D3 --> C11
    D3 --> H11
    A1 --> E
    F1 --> E
    
```