```mermaid

flowchart TD
    subgraph 国家海洋信息中心（核心节点）
        A[核心路由器] --> B[防火墙1（外网隔离）]
        B --> C[入侵检测系统（IDS）]
        C --> D[核心交换机]
        D --> E[国家应用服务器集群]
        D --> F[国家数据库服务器（PostgreSQL+Redis）]
        D --> G[中间件服务器集群（Kafka+Debezium）]
        A --> H[海洋专网骨干网]
    end
    
    subgraph 13个省市海洋局（以1个省市为例，其余12个架构一致）
        I[地方路由器] --> J[防火墙2（本地防护）]
        J --> K[地方交换机]
        K --> L[地方应用服务器]
        K --> M[地方数据库服务器（PostgreSQL+Redis）]
        I --> H[海洋专网骨干网]
    end
    
    H --> N[VPN加密网关（国家-地方加密传输）]
    O[公网] --> B[防火墙1（禁止公网直接访问）]
    P[地方办公终端] --> K[地方交换机]
    Q[国家办公终端] --> D[核心交换机]
    
```